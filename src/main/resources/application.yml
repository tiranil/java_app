spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/test_base
    username: postgres
    password: userpass
    driverClassName: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQL9Dialect
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true



